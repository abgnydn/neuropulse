{
  "name": "neuropulse",
  "version": "0.2.0",
  "description": "Neuropulse — a real forward pass of Phi-3-mini, rendered 1:1 from live WebGPU tensors, in your browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "verify:claims": "node tools/verify-claims.mjs",
    "verify:shortcuts": "node tools/check-shortcuts.mjs",
    "verify:recordings": "node tools/verify-recordings.mjs",
    "verify": "npm run verify:claims && npm run verify:shortcuts && npm run verify:recordings",
    "check": "npm run typecheck && npm run verify && npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/three": "^0.183.1",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "three": "^0.183.2"
  }
}
