# Butterfly compaction — moved

The Butterfly investigation is now a standalone, citable repository:

**→ [github.com/abgnydn/butterfly](https://github.com/abgnydn/butterfly)**

- Full writeup: [`PAPER.md`](https://github.com/abgnydn/butterfly/blob/main/PAPER.md)
  — "we falsified our own pre-registration twice, then confirmed something
  narrower." The headline: the compaction mechanism is a *content-shape
  adapter*, not a universal context manager.
- Harness, trained taggers, per-generation traces, and LongMemEval validation
  all live in that repo.

What stays in neuropulse:

- `src/butterfly-mode.ts` — the in-app Butterfly demo overlay (part of the
  visualizer).
- [`PREDICTIONS.md`](../PREDICTIONS.md), entries `P-20260512-05` and
  `P-20260515-06` — the git-timestamped pre-registrations, kept here as the
  canonical filing with their audit trail.
